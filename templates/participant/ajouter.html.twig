{% extends 'base.html.twig' %}

{% block title %}Nouveau participant{% endblock %}

{% block body %}
    <h1>Crée un nouveau participant</h1>

    {{ form_start(form) }}

    {{ form_widget(form) }}


    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}


    <a href="{{ path('participant_index') }}">Retour</a>
{% endblock %}
